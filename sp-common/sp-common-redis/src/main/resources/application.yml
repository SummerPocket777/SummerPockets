server:
  # 端口
  port: 8102
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://112.111.242.49:13020/db_test?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: db_test
    password: tWyiEEMG3RJiKZ7j
# MyBatis-Plus配置
mybatis-plus:
  # 指定mapper文件的位置
  mapper-locations: classpath:mapper/*.xml
  # 实体扫描，多个package用逗号或者分号分隔
  type-aliases-package: com.sp.model.domain
  # 全局的逻辑删除配置（下面3项配置，也可以在全局的配置类GlobalConfig中设置）
  global-config:
    db-config:
      id-type: auto        # 主键类型 AUTO:"数据库ID自增", INPUT:"用户输入ID", ID_WORKER:"全局唯一ID (数字类型唯一ID)", UUID:"全局唯一ID UUID";
      logic-delete-value: 1  # 逻辑已删除值(默认为1)
      logic-not-delete-value: 0  # 逻辑未删除值(默认为0)
############## Sa-Token 配置 (文档: https://sa-token.cc) ##############
#sa-token:
#  # token 名称（同时也是 cookie 名称）
#  token-name: satoken
#  # token 有效期（单位：秒） 默认30天，-1 代表永久有效
#  timeout: 2592000
#  # token 最低活跃频率（单位：秒），如果 token 超过此时间没有访问系统就会被冻结，默认-1 代表不限制，永不冻结
#  active-timeout: -1
#  # 是否允许同一账号多地同时登录 （为 true 时允许一起登录, 为 false 时新登录挤掉旧登录）
#  is-concurrent: true
#  # 在多人登录同一账号时，是否共用一个 token （为 true 时所有登录共用一个 token, 为 false 时每次登录新建一个 token）
#  is-share: true
#  # token 风格（默认可取值：uuid、simple-uuid、random-32、random-64、random-128、tik）
#  token-style: uuid
#  # 是否输出操作日志
#  is-log: true
#  #  开启内网服务鉴权
#  check-same-token: true
#
