<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sp.mapper.SysbusinessMapper">
    <select id="getYuyueList" resultType="com.sp.vo.BookVo">
        SELECT a.* , b.business_name FROM consumer_book a, sys_business b WHERE a.consumer_id = b.id AND a.user_id = #{userID} AND a.is_delete = 0 ORDER BY a.book_date DESC
    </select>
    <select id="getIdByName" resultType="java.lang.Integer">
        SELECT id FROM sys_business WHERE business_name = #{businessName}
    </select>
</mapper>